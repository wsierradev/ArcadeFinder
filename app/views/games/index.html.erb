<div class="small-6 small-centered columns">
<br>
<h1 class="bit-text arcade-header">Games</h1>
<% @sortedgames.each do |game| %>
<h4 class="smaller-text shadow">
  <%= link_to game.name.split('-').map(&:titleize).join('-'), game_path(game.id), class: "button expanded", style: "border-radius: 2px;"%>
</h4>
<%end%>


</div>
