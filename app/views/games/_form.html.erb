<%= form_for [@arcade,@game] do |f| %>
<% if game.errors.any? %>
 <div id="error_explanation">
   <h2><%= pluralize(game.errors.count, "error") %> prohibited this item from being saved:</h2>

   <ul>
     <% game.errors.full_messages.each do |message| %>
       <li><%= message %></li>
     <% end %>
   </ul>
 </div>
 <% end %>

 <%= f.label :name, class: "smaller-text"; %>
 <%= f.text_field :name %>
 <%= f.label :working, class: "smaller-text" %>
 <%= f.check_box :working%>
 <%= f.submit "Add Game", class: "button expanded smaller-text"%>
 <% end %>
